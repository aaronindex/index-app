// app/projects/components/ProjectFilterPills.tsx
'use client';

import { useRouter, useSearchParams } from 'next/navigation';

type Filter = 'all';

export default function ProjectFilterPills() {
  // Filter pills removed - all projects shown together
  // Users can create 1 project per client or 1 personal project if they want
  return null;
}

